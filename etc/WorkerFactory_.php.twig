<?php declare(strict_types=1);

// phpcs:disable
namespace Mammatus\Http\Server\Generated;

use ReactParallel\Logger\Logger;
use ReactParallel\Pool\Worker\Work;
use ReactParallel\Pool\Worker\Work\Worker;

/**
 * This class is generated by mammatus/http-server
 */
final class WorkerFactory_{{ server.vhost().name() }} implements Work\WorkerFactory
{
    private Logger $logger;

    public function __construct(Logger $logger)
    {
        $this->logger = $logger;
    }

    public function construct(): Worker
    {
        return new Worker_{{ server.vhost().name() }}($this->logger);
    }
}
// phpcs:enable
